# Настройка Google Sheets интеграции

Этот документ описывает настройку интеграции бота с Google Sheets для автоматического сохранения регистраций.

## 📋 Заголовки для Google Sheets

Добавьте следующие заголовки в первую строку листа "main":

| Столбец | Заголовок |
|---------|-----------|
| A1 | ID |
| B1 | Telegram ID |
| C1 | Username |
| D1 | Имя |
| E1 | Фамилия |
| F1 | Пакет участия |
| G1 | Участвовал ранее |
| H1 | Год участия |
| I1 | Выпускник ВШМ |
| J1 | Год выпуска |
| K1 | Дата регистрации |
| L1 | Дата обновления |

## 🔑 Настройка учетных данных

### 1. Создание сервисного аккаунта Google

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API и Google Drive API
4. Создайте сервисный аккаунт:
   - IAM & Admin → Service Accounts
   - Create Service Account
   - Скачайте JSON файл с ключами

### 2. Настройка доступа к таблице

1. Откройте скачанный JSON файл
2. Найдите поле `client_email`
3. Откройте вашу Google Sheets таблицу
4. Нажмите "Share" и добавьте email сервисного аккаунта с правами "Editor"

### 3. Конфигурация бота

Добавьте в файл `.env`:
```env
GOOGLE_CREDENTIALS_PATH=credentials.json
GOOGLE_SPREADSHEET_URL=https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID/edit
```

Где:
- `credentials.json` - путь к файлу с ключами сервисного аккаунта
- `YOUR_SPREADSHEET_ID` - ID вашей таблицы из URL

## 🧪 Тестирование

Запустите тест интеграции:
```bash
python3 test_google_sheets.py
```

## 📊 Возможности

- **Автоматическое добавление** новых регистраций
- **Обновление существующих** записей при повторной регистрации
- **Поиск по Telegram ID** для предотвращения дублей
- **Форматирование данных** для удобного чтения
- **Статистика** из Google Sheets

## 🔧 Структура данных

Каждая строка содержит:
- **ID** - уникальный идентификатор из БД
- **Telegram ID** - ID пользователя в Telegram (для поиска дублей)
- **Username** - @username из Telegram
- **Имя/Фамилия** - персональные данные
- **Пакет участия** - с ценой и эмодзи
- **Участие/Выпуск** - история пользователя
- **Временные метки** - даты регистрации и обновления

## ⚠️ Важные замечания

1. **Безопасность**: Не коммитьте файл `credentials.json` в git
2. **Права доступа**: Сервисный аккаунт должен иметь права Editor
3. **Лимиты API**: Google Sheets API имеет лимиты запросов
4. **Обработка ошибок**: Ошибки Google Sheets не останавливают бота

## 🚀 Запуск

После настройки запустите бота обычным способом:
```bash
python3 main.py
```

Бот автоматически:
- Подключится к Google Sheets при старте
- Будет добавлять новых пользователей
- Обновлять существующие записи
- Логировать все операции